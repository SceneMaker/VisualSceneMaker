Supernode "WaitSupernode" waits and exits via interrupt edge when the event "OkayButtonPressed" occurs and Node "After" is activated. The supernode is kept alive by a self timeout edge every 1000 ms.
